/var/log/hashbash/nginx-*.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    dateext
    postrotate
        [ -f /run/hashbash-frontend/nginx.pid ] && kill -USR1 `cat /run/hashbash-frontend/nginx.pid`
    endscript
}
