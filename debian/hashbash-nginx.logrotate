/opt/hashbash/logs/nginx-*.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    dateext
    postrotate
        [ -f /run/hashbash-nginx.pid ] && kill -USR1 `cat /run/hashbash-nginx.pid`
    endscript
}
